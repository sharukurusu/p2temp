<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand">Groove Rooms</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                {{#if logged-in}}
                <a class="nav-item nav-link" href="/members">Profile{{#if profile}}<span class="sr-only">(current){{/if}}</a>
                <a class="nav-item nav-link" href="/chat">Chat{{#if chat}}<span class="sr-only">(current){{/if}}</span></a>
                <a class="nav-item nav-link navbar-right" href="/logout">Logout</a>
                {{else}}
                <a class="nav-item nav-link" href="/login">Login{{#if login}}<span class="sr-only">(current){{/if}}</a>
                <a class="nav-item nav-link" href="/">Register{{#if register}}<span class="sr-only">(current){{/if}}</a>
                {{/if}}
            </div>
        </div>
</nav>